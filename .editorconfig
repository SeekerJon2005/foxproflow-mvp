root = true

# Общие правила
[*]
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true

# Код и текстовые файлы → LF
[*.{py,md,txt,json,jsonl,sql,yml,yaml,csv,sh,svg}]
end_of_line = lf

# Windows-скрипты → CRLF
[*.{bat,cmd,ps1}]
end_of_line = crlf
