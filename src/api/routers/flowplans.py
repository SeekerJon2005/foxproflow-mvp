from fastapi import APIRouter

router = APIRouter(tags=["flowplans"])

# Stub router.
# Purpose: prevent startup warnings "Skip router api.routers.flowplans: ModuleNotFoundError"
# Placeholder for future FlowPlans API endpoints.
